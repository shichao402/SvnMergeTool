1. 缺少mergeinfo 导致不在本体系下合并的记录无法被识别.
2. svn的缓存数据, 要分库. 一个url是一个库. 可以共享. 比如trunk这个源, 我们会把他合并到很多分支. 那我们操作过程中. 实际上缓存的svn日志都是来自trunk的, 他就只需要一份数据库来保存. 和要合并到哪里没有关系.
3. 把日志设计放到左上角的设置按钮中(新加一个). 老的这个设置就不要了.然后设置界面是全屏的. 现在主界面的最大重试次数也放到这个设置界面里.设置界面要按功能类型分一下组. 
4. svn日志列表里的最后一页按钮可以加回来了. 然后下一页的功能可以不再耦合找svn日志服务获取更多日志的功能. 以为我们已经有了自动缓存功能和加载全部功能.
